import jsPrimitives from '../../../utils/js-primitives';
import { module, test } from 'qunit';

module('Unit | Utility | js primitives');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = jsPrimitives();
  assert.ok(result);
});
