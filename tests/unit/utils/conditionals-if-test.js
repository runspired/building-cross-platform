import conditionalsIf from '../../../utils/conditionals-if';
import { module, test } from 'qunit';

module('Unit | Utility | conditionals if');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = conditionalsIf();
  assert.ok(result);
});
