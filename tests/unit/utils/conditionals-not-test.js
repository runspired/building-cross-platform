import conditionalsNot from '../../../utils/conditionals-not';
import { module, test } from 'qunit';

module('Unit | Utility | conditionals not');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = conditionalsNot();
  assert.ok(result);
});
