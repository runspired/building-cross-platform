import jsVars from '../../../utils/js-vars';
import { module, test } from 'qunit';

module('Unit | Utility | js vars');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = jsVars();
  assert.ok(result);
});
