import conditionalsOr from '../../../utils/conditionals-or';
import { module, test } from 'qunit';

module('Unit | Utility | conditionals or');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = conditionalsOr();
  assert.ok(result);
});
