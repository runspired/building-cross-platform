import moduleImport from '../../../utils/module-import';
import { module, test } from 'qunit';

module('Unit | Utility | module import');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = moduleImport();
  assert.ok(result);
});
