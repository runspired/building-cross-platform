import conditionalsAnd from '../../../utils/conditionals-and';
import { module, test } from 'qunit';

module('Unit | Utility | conditionals and');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = conditionalsAnd();
  assert.ok(result);
});
